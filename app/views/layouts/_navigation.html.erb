<div class="navbar navbar-default">
  <!--div class="ribbon-wrapper-green"><div class="ribbon-green">alpha version</div></div-->

  <div class="navbar-inner">

<% if !in_app? %>
    <!-- Responsive Navbar Part 1: Button for triggering responsive navbar (not covered in tutorial). Include responsive CSS to utilize. -->
    <!--button type="button" class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button-->
<% end %>

<% if in_app? && user_signed_in? %>
    <ul class="nav navbar-nav navbar-left">
      <li class="home"><a href="" ng-click="selected.emotion = []"><svginline ng-class="{on: selected.emotion.length == 0, off: selected.emotion.length > 0}" src="'<%=image_path('FacetsKidsAlpha2.svg')%>'"></svginline></a></li>
    </ul>
<% else %>
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to image_tag("FacetsKids2alpha.png"), unauthenticated_root_path, :class => 'navbar-brand' %>
    </div>
<% end %>

<% if in_app? && user_signed_in? %>
    <ul class="nav navbar-nav navbar-left" style="" svgbuttongroup tag-list="selected.emotion" multiple="false">
      <li class="tag"><svgbutton src="<%=image_path('Happy2.svg')%>" value="happy" onclick="snapper.close();"></svgbutton></li>
      <li class="tag"><svgbutton src="<%=image_path('Excited2.svg')%>" value="excited" onclick="snapper.close();"></svgbutton></li>
      <li class="tag"><svgbutton src="<%=image_path('Scared2.svg')%>" value="scared" onclick="snapper.close();"></svgbutton></li>
      <li class="tag"><svgbutton src="<%=image_path('Think2.svg')%>" value="think" onclick="snapper.close();"></svgbutton></li>
      <li class="tag"><svgbutton src="<%=image_path('Laugh2.svg')%>" value="laugh" onclick="snapper.close();"></svgbutton></li>
      <li class="tag"><svgbutton src="<%=image_path('Care2.svg')%>" value="care" onclick="snapper.close();"></svgbutton></li>
    </ul>
    <ul class="nav navbar-nav navbar-right">
      <li><%= link_to "", destroy_user_session_path + "?check=1", :method => 'delete', :class => 'icon-logout logout'%></li>
    </ul>
<% end %>
 
<% if !in_app? %>
    <!-- Responsive Navbar Part 2: Place all navbar contents you want collapsed withing .navbar-collapse.collapse. -->
    <div class="navbar-collapse collapse">
      <ul class="nav navbar-nav navbar-right">
  <% if user_signed_in? %>
        <li><%= link_to 'Log out', destroy_user_session_path, :method => 'delete' %></li>
  <% else %>
        <li><%= link_to "Sign up", new_user_registration_path %></li>
  <% end %>
        <li><%= link_to 'Manage my account', edit_user_registration_path %></li>
        <li><%= link_to "About", content_faq_path %></li>
      </ul>
    </div><!--/.nav-collapse -->
<% end %>

  </div><!-- /.navbar-inner -->
</div><!-- /.navbar --> 


