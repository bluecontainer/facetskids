
<div style="padding-top: 20px;">

<div class="row">
  <div class="offset1 span8">
    <h3>Facets Kids</h3>
    <p>
    Thank you! We will notify you by email as soon as Facets Kids is available for any of the devices you selected. 
    </p>
    <% if !current_user.nil? %>
    <p>
    In the meantime, share your Facets Kids discovery and help us spread the word!
    </p>
    <% end %>
  </div>
</div>

<% if !current_user.nil? %>
<%= render "layouts/share_with" %>
<% end %>

<%= render "layouts/fb" %>

</div>

